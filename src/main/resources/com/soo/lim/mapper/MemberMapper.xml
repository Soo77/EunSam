<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.soo.lim.dao.MemberDao">

    <!-- 컬럼 이름과 자바 객체의 프로퍼티 이름을 연결한다. -->
    <resultMap type="Member" id="MemberMap">
        <id column="member_id" property="memberNo"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="address" property="address"/>
        <result column="address_detail" property="addressDetail"/>
        <result column="card_no" property="cardNo"/>
        <result column="email" property="email"/>
        <result column="mobile_no" property="mobileNo"/>
    </resultMap>

    <select id="findAll" resultMap="MemberMap">
    select * from member
  </select>

    <insert id="insert" parameterType="Member">
    insert into member
    (
    member_id,
    name,
    password,
    email
    )
    values
    (
    #{memberNo},
    #{name},
    password(#{password}),
    #{email}
    )
  </insert>

    <select id="findByIdPassword" parameterType="map" resultMap="MemberMap">
    select
    member_id,
    name,
    address,
    email,
    mobile_no
    from member
		where member_id =
		#{memberId} and password = password(#{password})
  </select>

</mapper>